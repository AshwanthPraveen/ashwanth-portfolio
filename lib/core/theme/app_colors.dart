// ============================================================================
// File: app_colors.dart
// Created Date: 16-Sep-2026
// Title: AppColors
// Description:
//   Defines all color values used throughout the application.
//
// Class:
//   AppColors
//
// Author: Ashwanth V Praveen
// ============================================================================

import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const background = Color(0xFF070A12);
  static const surface = Color(0xFF0D1220);
  static const surfaceElevated = Color(0xFF12192A);

  // Brand
  static const primary = Color(0xFF6C63FF);
  static const primaryLight = Color(0xFF8B85FF);
  static const accent = Color(0xFF38BDF8);

  // Text
  static const textPrimary = Color(0xFFF5F7FA);
  static const textSecondary = Color(0xFFA5ADBD);
  static const textMuted = Color(0xFF687386);

  // Borders & States
  static const border = Color(0xFF1E293B);
  static const success = Color(0xFF22C55E);
  static const error = Color(0xFFEF4444);
}
